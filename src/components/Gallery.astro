<section id="gallery" class="gallery-section">
  <h3 class="section-title">Galería y Videos</h3>
  
  <div class="gallery-container">
    

    
    <div class="gallery-item">
        <img src="/images/1.jpg" alt="Discoteca" class="gallery-image" />
    </div>
    <div class="gallery-item">
        <img src="/images/2.jpg" alt="Interior" class="gallery-image" />
    </div>
    <div class="gallery-item">
        <img src="/images/3.jpg" alt="Evento en vivo" class="gallery-image" />
    </div>
    <div class="gallery-item">
        <img src="/images/4.jpg" alt="Detalle de luces" class="gallery-image" />
    </div>
    
  </div>
</section>

<style>
.gallery-section {
  padding: 80px 20px;
  background: #000;
  text-align: center;
}

.gallery-section .section-title {
  font-size: 2.5em;
  margin-bottom: 40px;
  color: #bebebb;
  text-transform: uppercase;
  letter-spacing: 2px;
}

.gallery-container {
  display: flex;
  justify-content: center;
  gap: 20px;
  flex-wrap: wrap;
}


.gallery-item {
  width: 320px; 
  height: 220px; 
  overflow: hidden; 
  border-radius: 10px;
  transition: transform 0.4s ease, box-shadow 0.4s ease;
  cursor: pointer;
  position: relative;
}

/* Efecto Hover Elegante */
.gallery-item:hover {
  transform: scale(1.05); /* Se agranda ligeramente */
  box-shadow: 0 10px 30px rgba(255, 215, 0, 0.4); /* Sombra dorada sutil */
}

/* ESTILOS DE LAS IMÁGENES               */

.gallery-image {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: opacity 0.4s;
}


/* ESTILOS DEL VIDEO DE INSTAGRAM        */

.video-item {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  background-color: #1a1a1a; /* Fondo oscuro para el video */
}

.instagram-video {
  width: 100%;
  height: 100%; /* El iframe toma el 100% del contenedor gallery-item */
  display: block;
}

.video-caption {
    position: absolute;
    bottom: 5px;
    color: #ccc;
    font-size: 0.9em;
    padding: 5px 0;
    width: 100%;
    background: rgba(0, 0, 0, 0.6);
}

/* Media Query para Responsiveness */
@media (max-width: 768px) {
  .gallery-item {
    width: 90%; /* Hace que las imágenes y el video ocupen casi todo el ancho en móvil */
    height: 250px;
  }
}
</style>
