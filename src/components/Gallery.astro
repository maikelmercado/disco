<section id="gallery" class="gallery-section">
  <h3 class="section-title">Fiestas</h3>
  
  <div class="gallery-container">
    <div class="carousel-track">
        <div class="gallery-item"><img src="public/img/MACANUDO Sábado 24-02 SANTO DOMINGO 2024 (1).jpg" alt="Discoteca" class="gallery-image" /></div>
        <div class="gallery-item"><img src="public/img/MACANUDO Sábado 24-02 SANTO DOMINGO 2024 (2).jpg" alt="Discoteca" class="gallery-image"/></div>
        <div class="gallery-item"><img src="public/img/MACANUDO Sábado 24-02 SANTO DOMINGO 2024 (3).jpg" alt="Discoteca" class="gallery-image" /></div>
        <div class="gallery-item"><img src="public/img/MACANUDO Sábado 24-02 SANTO DOMINGO 2024.jpg" alt="Discoteca" class="gallery-image" /></div>
        
        <div class="gallery-item"><img src="public/img/MACANUDO Sábado 24-02 SANTO DOMINGO 2024 (1).jpg" alt="Discoteca" class="gallery-image" /></div>
        <div class="gallery-item"><img src="public/img/MACANUDO Sábado 24-02 SANTO DOMINGO 2024 (2).jpg" alt="Discoteca" class="gallery-image"/></div>
        <div class="gallery-item"><img src="public/img/MACANUDO Sábado 24-02 SANTO DOMINGO 2024 (3).jpg" alt="Discoteca" class="gallery-image" /></div>
        <div class="gallery-item"><img src="public/img/MACANUDO Sábado 24-02 SANTO DOMINGO 2024.jpg" alt="Discoteca" class="gallery-image" /></div>
    </div>
  </div>
</section>
<style>
/* --- ESTILOS DEL CONTENEDOR PRINCIPAL Y TÍTULO --- */

.gallery-section {
  padding: 80px 20px;
  background: #000; /* Fondo negro */
  text-align: center;
}

.gallery-section .section-title {
  font-size: 3em; /* Título más grande */
  margin-bottom: 50px;
  color: #fff; /* Título blanco moderno */
  text-transform: uppercase;
  letter-spacing: 4px;
  text-shadow: 0 0 15px rgba(255, 255, 255, 0.4);
}

/* --- CONTENEDOR DE LA GALERÍA --- */

.gallery-container {
  display: flex;
  justify-content: center;
  /* El nuevo ancho máximo para 4 fotos (380*4 + 20*4) es 1600px */
  max-width: 1600px; 
  margin: 0 auto; 
  overflow: hidden; 
}

/* --- ÍTEM DE LA GALERÍA (La Foto) --- */

.gallery-item {
  /* FOTOS MÁS GRANDES */
  width: 380px; 
  height: 280px; 
  flex-shrink: 0; 
  overflow: hidden; 
  
  /* Borde más curvo y moderno */
  border-radius: 25px; 
  
  /* Borde sutil */
  border: 3px solid rgba(255, 255, 255, 0.1); 
  
  transition: transform 0.4s ease, box-shadow 0.4s ease;
  cursor: pointer;
  position: relative;
}

.gallery-item:hover {
  transform: scale(1.05); 
  /* Sombra más intensa y brillante al pasar el ratón */
  box-shadow: 0 15px 45px rgba(135, 206, 250, 0.6); 
}

/* --- IMAGEN DENTRO DEL ÍTEM --- */

.gallery-image {
  width: 100%;
  height: 100%;
  object-fit: cover; 
  transition: opacity 0.4s;
}

/* --- PISTA DE CARRUSEL (La que se mueve) --- */

.carousel-track {
  display: flex;
  gap: 20px; 
  /* Velocidad un poco más rápida para el tamaño más grande */
  animation: scroll-slide 25s linear infinite; 
}

.gallery-container:hover .carousel-track {
    animation-play-state: paused; 
}


/* --- DEFINICIÓN DE LA ANIMACIÓN (Desktop) --- */
@keyframes scroll-slide {
  0% {
    transform: translateX(0);
  }
  100% {
    /* Nuevo desplazamiento: 4x 380px + 4x 20px = 1520px + 80px = 1600px */
    transform: translateX(-1600px); 
  }
}

/* --- MEDIA QUERY (Móviles) --- */
@media (max-width: 768px) {
  .gallery-item {
    width: 90vw; 
    height: 60vw; /* Usamos 'vw' también para la altura para mejor responsividad */
    border-radius: 15px; /* Bordes un poco menos pronunciados en móvil */
  }
  
  @keyframes scroll-slide {
      0% {
        transform: translateX(0);
      }
      100% {
        /* Desplazamiento en móvil: 4x 90vw + 4x 20px = 360vw + 80px */
        transform: translateX(calc(-360vw - 80px)); 
      }
  }
}
</style>