<header class="header">
  <div class="logo-container">
    <img src="/img/logo double.jpg" alt="Logo" class="logo">
  </div>
  
  <nav id="nav" class="nav-menu">
    <a href="#events" class="nav-link">Eventos</a>
    <a href="#gallery" class="nav-link">Galería</a>
    <a href="#contact" class="nav-link">Contacto</a>
  </nav>
  
  <div id="hamburger" class="hamburger">
    <div class="bar"></div>
    <div class="bar"></div>
    <div class="bar"></div>
    <div class="bar"></div>
  </div>
</header>

<div id="mobileMenu" class="mobile-menu hidden">
  <a href="#events" class="mobile-link">Eventos</a>
  <a href="#gallery" class="mobile-link">Galería</a>
  <a href="#contact" class="mobile-link">Contacto</a>
</div>


<script>
 
  const header = document.querySelector('.header');
  const hamburger = document.getElementById('hamburger');
  const mobileMenu = document.getElementById('mobileMenu');
  const navLinks = mobileMenu?.querySelectorAll('a');
  const scrollThreshold = 100; 

 
  if (hamburger && mobileMenu) {
    

    hamburger.addEventListener('click', () => {
      mobileMenu.classList.toggle('hidden');
      hamburger.classList.toggle('is-active'); 
    });
    
    navLinks?.forEach(link => {
        link.addEventListener('click', () => {
            mobileMenu.classList.add('hidden');
            hamburger.classList.remove('is-active');
        });
    });

    // c) Efecto Scroll (Cambio de color del Header)
    if (header) {
      window.addEventListener('scroll', () => {
        if (window.scrollY > scrollThreshold) {
          header.classList.add('scrolled');
        } else {
          header.classList.remove('scrolled');
        }
      });
    }
  }
</script>


<style>

/* ESTILOS DEL HEADER Y BASE */
body {
    font-family: 'Montserrat', sans-serif;
    margin: 0;
    /* Ajuste de relleno para el header fijo */
    padding-top: 105px; 
    /* Necesario para probar el scroll */
    min-height: 200vh; 
}

.header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20px 50px;
  /* FONDO TRANSPARENTE INICIAL */
  background: rgba(0, 0, 0, 0); 
  position: fixed;
  width: 100%;
  top: 0;
  z-index: 999;
  box-sizing: border-box; 
  transition: background 0.5s ease, box-shadow 0.5s ease;
}

/
.header.scrolled {
  background: #000000; 
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.5);
}


/* LOGO */
.logo-container {
    display: flex;
    align-items: center;
}

.logo {
  height: 65px; 
  width: 65px;  
  border-radius: 50%; 
  object-fit: cover; 
}


/* MENÚ DE NAVEGACIÓN (Desktop)*/
.nav-menu {

  display: flex; 
  align-items: center;
}

.nav-link {
  margin-left: 15px; 
  text-decoration: none;
  color: #fff;
  font-family: 'Montserrat', sans-serif;
  font-size: 1rem; 
  font-weight: 500; 
  letter-spacing: 0px; 
  text-transform: uppercase;
  transition: color 0.3s;
}

.nav-link:hover {
  color: #666665;
}

/* ESTILOS HAMBURGUESA Y BARRAS */
.hamburger {
  /* Inicialmente oculto en escritorio */
  display: none; 
  flex-direction: column;
  cursor: pointer;
  padding: 5px;
  transition: transform 0.4s ease; 
  z-index: 1000;
}

.bar {
  width: 28px; 
  height: 4px; 
  background: #fff; 
  margin: 3px 0; 
  transition: 0.4s;
  border-radius: 2px; 
}

/* Animación 'X' */
.hamburger.is-active .bar:nth-child(1) {
  transform: translateY(7px) rotate(45deg);
}

.hamburger.is-active .bar:nth-child(2) {
  opacity: 0;
}

.hamburger.is-active .bar:nth-child(3) {
  transform: translateY(-7px) rotate(-45deg);
}

.hamburger.is-active .bar:nth-child(4) {
    display: none; 
}

/* MENÚ MÓVIL */
  .mobile-menu {
      position: fixed;
      top: 105px; 
      left: 0;
      width: 100%;
      background: rgba(0, 0, 0, 0.95); 
      z-index: 998;
      max-height: 50vh;
      overflow: auto;
      opacity: 1;
      transition: all 0.3s ease-in-out;
  }

  .mobile-menu.hidden {
      max-height: 0;
      padding: 0;
      overflow: hidden;
      opacity: 0;
  }

  .mobile-link {
      display: block;
      text-align: center;
      padding: 15px 0;
      text-decoration: none;
      color: #fff;
      font-size: 1.1rem;
      font-weight: 500;
      text-transform: uppercase;
      transition: background 0.3s;
  }

  .mobile-link:hover {
      background: #1a1a1a;
      color: #ffcc00;
  }


  /* MEDIA QUERIES (Responsividad) */
  @media (max-width: 768px) {
    
    .header {
      padding: 15px 25px;
      height: 80px; 
    }

    /*menú de escritorio se oculte */
    .nav-menu {
      display: none !important; 
    }

    /* MOSTRAR HAMBURGUESA */
    .hamburger {
      display: flex; 
    }
  

  